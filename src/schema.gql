# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: String!
  username: String!
  password: String!
  reclamos: [Reclamo!]!
}

type Reclamo {
  id: String!
  nro: Int!
  descripcion: String!
  detalleDeCompra: String!
  problema: String!
  user: User!
}

type LoginResponse {
  accessToken: String!
  user: User!
}

type Query {
  reclamos(paginationDTO: PaginationDTO!): [Reclamo!]!
  reclamo(id: String!): Reclamo!
  reclamosPorPalabra(palabra: String!): [Reclamo!]!
  users(paginationDTO: PaginationDTO!): [User!]!
  user(id: String!): User!
  userByUsername(username: String!): User!
}

input PaginationDTO {
  limit: Int
  offset: Int
}

type Mutation {
  createReclamo(createReclamoDTO: CreateReclamoDTO!): Reclamo!
  updateReclamo(id: String!, updateReclamoDTO: UpdateReclamoDTO!): Reclamo!
  deleteReclamo(id: String!): Boolean!
  createUser(createUserDTO: CreateUserDTO!): User!
  updateUser(updateUserDTO: UpdateUserDTO!, id: String!): User!
  removeUser(id: String!): User!
  login(loginUserInput: LoginInput!): LoginResponse!
  register(CreateUserInput: CreateUserDTO!): User!
}

input CreateReclamoDTO {
  nro: Int!
  descripcion: String!
  detalleDeCompra: String!
  problema: String!
  idUser: String
  user: CreateUserDTO
}

input CreateUserDTO {
  username: String!
  password: String!
}

input UpdateReclamoDTO {
  nro: Int
  descripcion: String
  detalleDeCompra: String
  problema: String
}

input UpdateUserDTO {
  username: String
  password: String
}

input LoginInput {
  username: String!
  password: String!
}